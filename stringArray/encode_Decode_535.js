
class Solution{
    var encode = function(longUrl) {
    
    };
    
    
    var decode = function(shortUrl) {
        
    };
    
}

const url = "https://leetcode.com/problems/design-tinyurl";
const obj = new Solution();
let tiny  = obj.encode(url);
let ans   = obj.decode(tiny);
return ans;